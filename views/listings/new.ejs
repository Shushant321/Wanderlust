<div class="new-listing-container">
  <h3 class="new-listing-title">Create a New Listing</h3>
  <form method="POST" action="/listings" novalidate class="needs-validation" enctype="multipart/form-data">
    <div>
      <input name="listing[title]" class="new-listing-input" type="text" placeholder="Title" required />
      <div class="invalid-feedback">Please provide a Title.</div>
    </div>
    <div>
      <textarea name="listing[description]" class="new-listing-input" rows="3" placeholder="Description" required></textarea>
      <div class="invalid-feedback">Please provide a Description.</div>
    </div>
    <div>
      <input name="listing[image]" class="new-listing-input" type="file" />
    </div>
    <div>
      <input name="listing[price]" class="new-listing-input" type="number" placeholder="Price" required />
      <div class="invalid-feedback">Please provide a Price.</div>
    </div>
    <div>
      <input name="listing[country]" class="new-listing-input" type="text" placeholder="Country" required />
      <div class="invalid-feedback">Please provide a Country.</div>
    </div>
    <div>
      <input name="listing[location]" class="new-listing-input" type="text" placeholder="Location" required />
      <div class="invalid-feedback">Please provide a Location.</div>
    </div>
    <button type="submit" class="new-listing-btn">Add Listing</button>
  </form>
</div>

<style>
  /* Desktop Airbnb-style new listing form */
.new-listing-container {
  width: 600px;
  margin: 40px auto;
  background: #fff;
  padding: 36px 40px 30px 40px;
  box-shadow: 0 4px 24px rgba(60,64,67,0.10);
  border-radius: 24px;
  display: block;
}

.new-listing-title {
  text-align: center;
  margin-bottom: 28px;
  color: #222;
  font-size: 2rem;
  font-weight: 700;
  letter-spacing: 0.01em;
}

.new-listing-input {
  width: 100%;
  padding: 14px 16px;
  margin: 12px 0;
  border: 1.5px solid #eee;
  border-radius: 16px;
  font-size: 1.08rem;
  background: #fafafa;
  transition: border-color 0.2s, box-shadow 0.2s;
  box-sizing: border-box;
}

.new-listing-input:focus {
  border-color: #FF5A5F;
  background: #fff;
  box-shadow: 0 0 0 2px rgba(255,90,95,0.10);
  outline: none;
}

.new-listing-btn {
  background: #FF5A5F;
  color: #fff;
  padding: 14px 0;
  border: none;
  border-radius: 999px;
  font-size: 1.13rem;
  font-weight: 600;
  width: 100%;
  margin-top: 18px;
  box-shadow: 0 2px 8px rgba(255,90,95,0.08);
  transition: background 0.2s;
  letter-spacing: 0.01em;
}
.new-listing-btn:hover,
.new-listing-btn:focus {
  background: #e31c5f;
  color: #fff;
}

/* Responsive: Airbnb mobile look */
@media (max-width: 768px) {
  .new-listing-container {
    width: 98vw;
    margin: 16px auto;
    padding: 18px 7vw 22px 7vw;
    border-radius: 16px;
    box-shadow: 0 2px 12px rgba(60,64,67,0.10);
  }
  .new-listing-title {
    font-size: 1.25rem;
    margin-bottom: 18px;
  }
  .new-listing-btn {
    font-size: 1.05rem;
    padding: 13px 0;
    border-radius: 24px;
  }
  .new-listing-input {
    border-radius: 12px;
    font-size: 1rem;
    padding: 11px 12px;
  }
}

</style>